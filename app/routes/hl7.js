import Route from '@ember/routing/route';

export default Route.extend({
    model() {
        //let jsonPromise = Ember.$.getJSON("/assets/changes.json");
        //console.log(jsonPromise);        
    }
});
